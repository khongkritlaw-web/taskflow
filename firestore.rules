rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global safety net: allow read/write to facilitate direct sync and login flows across devices
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
